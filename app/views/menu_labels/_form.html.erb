<%= form_for(@menu_label) do |f| %>
  <% if @menu_label.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu_label.errors.count, "error") %> prohibited this menu_label from being saved:</h2>

      <ul>
      <% @menu_label.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :menu_label %><br />
    <%= f.text_field :menu_label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
