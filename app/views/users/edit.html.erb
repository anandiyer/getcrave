<% if current_user.id == @user.id %>
	<h1>Edit your profile</h1>
	<%= render 'form' %>

	<%= link_to 'Show', @user %> |
	<%= link_to 'Back', users_path %>
<% else %>
	Whoa you're not allowed to do that!
<% end %>