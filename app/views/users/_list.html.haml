#reviews_wrapper{:class => @controller.controller_name}
  = render :partial => "users/buttons"

  - if !controller.action_name.include? "follow"
    #select_box_hint &nbsp;
    #sorting
      #before_sort_by
        Sort by:
      #sort_by
        =text_field(:select, :sorttype, :value => "Newest", :disabled => true)
        %ul
          -["Rating", "Other"].each do |x|
            %li= x
  #reviews_wrapper{:class => class_name_from_path}
    #update_place_restaurants{:class => class_name_from_path}

      = render :partial => "/user_items" if @controller.action_name == "show"
      = render :partial => "/items_grouped_by_stars" if @controller.action_name == "saved"
      = render :partial => "following_users" if @controller.action_name.include? "follow"

      -#= render :partial => "/show_more"