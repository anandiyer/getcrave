#reviews_wrapper_bloc_wrapper

  - @menu_item ||= MenuItem.find(params[:id])
  - @current_menu_item_reviews = @menu_item.menu_item_ratings.reverse
  - @show_more_step = 10


  -if !params[:limit]
    - review_limit = @show_more_step
  - else
    - review_limit = params[:limit].to_i

  - @current_menu_item_reviews_limits = @current_menu_item_reviews.first(review_limit)
  - @current_menu_item_reviews_limits.each do |@mi_rating|
    - user = User.find(@mi_rating.user_id)

    - if @mi_rating.user_id?
      .reviews_wrapper_block
        .review_image_col
          =image_tag "reviews_avatar_temp.png"
          #click &nbsp;

        .review_desc_col_wrapper
          .before_start
            .data_review
              =@mi_rating.updated_at.strftime("%b %d, %Y")
            %h3
              -#TODO: refactor to somethings like @mi_rating.user.user_name
              =user.user_name
            %em
              by
              =link_to user.user_name, user
          -rating = @mi_rating.rating ? @mi_rating.rating : 0
          .star_sm{:class => "star_sm stars_"+@mi_rating.rating.to_s.gsub(/[.]/, '_'), :id => @mi_rating.id, :data => {:rating => rating, :model => "menu_item_ratings"}} &nbsp;
          .review_body_head
            =@mi_rating.review
            -#=link_to "Show more", "#"
          .helpful_block
            .before_answer_block
              Was this review helpful?

              -#- begin
              -#  - u = current_user
              -#  - u.user_saved_menu_items
              -#  - u.menu_item_ratings.count
              -#  = u.menu_item_ratings.find(258).exists?
              -#  -#=  u.menu_item_ratings.find(@mi_rating.id).menu_item_rating_helpfuls.count
              -#- rescue ActiveRecord::RecordNotFound
              -#  error

              -#= u.menu_item_ratings.find(258).menu_item_rating_helpfuls.count
            %span.yes_answer
              -#=User.first().menu_item_ratings.find(257)
              -#u.menu_item_ratings.find(258).menu_item_rating_helpfuls.exists?
              #link_here_helpfull
                %span= link_to "Yes","#"
                = form_for(MenuItemRatingHelpful.new(@mi_rating.id), :remote => true) do |f|
                  = f.hidden_field :menu_item_rating_id, :value => @mi_rating.id
                  = f.hidden_field :user_id, :value => current_user.id if current_user
            .found_helpfull
              =" (" + @mi_rating.menu_item_rating_helpfuls.count.to_s+")"
<<<<<<< HEAD
=link_to("#limit="+(@show_more_step*2).to_s) do
  #show_more_button{:data =>{:step => @show_more_step, :next => @show_more_step*2, :itemid => params[:id]}}
    Show more
=======
/
  =link_to("#limit="+(@show_more_step*2).to_s) do
    #show_more_button{:data =>{:step => @show_more_step, :next => @show_more_step*2, :itemid => params[:id]}}
      Show more
>>>>>>> 4250b2f0c9aa187ae210893a947cd03fda7ab6d2
