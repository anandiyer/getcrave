- if @controller.controller_name != "user_saved_menu_items"
  = render :partial => "/nearby_buttons"
- else
  %h2 Saved Dishes

#reviews_wrapper{:class => @controller.controller_name+"_"+@controller.action_name}
  #update_place_restaurants{:class => @controller.controller_name+"_"+@controller.action_name}
    = render :partial => "/items_grouped_by_stars"
