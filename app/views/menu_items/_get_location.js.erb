if (navigator.geolocation) {
  navigator.geolocation.getCurrentPosition(success, error);
} else {
  error('not supported');

}

function success(position) {

// console.log(position.coords.latitude)
// console.log(position.coords.longitude)
    if(window.location.port!=3006){
        window.location = "/items/location?lat="+position.coords.latitude+"&long="+position.coords.longitude
    }else{
        error("Error!")
    }
}

function error(msg) {
  console.log(arguments);
    // Redirect to Crave's home :)
    window.location = "/items/location?lat=37.77868&long=-122.39151"
}