module RestaurantsHelper

#  TODO: please finish me
  def is_restaurant_saved? restaurant_id
#    UserSavedMenuItem.where(:user_id => current_user.id, :menu_item_id => menu_item_id).exists? if current_user
    false
  end

end
